# 开发记录

## 2026-03-20

### 已完成

- 明确平台定位：内部项目跟踪、过程晾晒、OKR 对齐、透明协作
- 完成《项目管理平台需求文档》
- 完成《详细流程业务流程设计》
- 完成页面设计文档
- 完成 `PageDesign/` 目录下的静态 HTML 草图
- 强化页面中的任务层级设计：
  - 主任务
  - 子任务
  - 技术线子任务
  - 执行人分配
  - 附件与素材展示
- 建立 `docs/` 文档体系
- 初始化 `backend/` FastAPI 骨架
- 初始化 `frontend/` Vue3 骨架
- 初始化 `deploy/docker-compose.yml`
- 补充 SQLite、文件型数据库、Redis 的基础连接配置骨架
- 完成后端 Python 语法编译检查
- 完成前端依赖安装
- 完成前端生产构建验证
- 完成登录功能第一版
  - 支持用户名 + 密码登录
  - 支持手机号 + 密码登录
- 完成系统一、二级菜单骨架
- 完成用户管理功能第一版
  - 用户创建
  - 用户修改
  - 用户删除
  - 板块权限设置
- 完成后端接口测试验证
- 将开发阶段存储方案切换为 SQLite + 文件型数据库
- 按方案 B 完成登录与权限体系升级
  - JWT 登录态
  - SQLite 持久化用户、角色、权限
  - 基础 RBAC 模型
  - 前端按角色分配权限
- 按方案 B 完成项目管理第一版
  - 管理员创建项目
  - 项目节点初始化
  - 项目成员范围控制
  - 项目成员创建任务
  - 任务更新进度与负责人
  - 任务仅支持废弃，不支持物理删除
  - 任务评论与 @提醒
  - 任务附件上传下载
  - 手动提醒与站内通知
  - 操作日志留痕
  - 前端项目列表页与项目详情页联调完成
- 新增项目管理接口测试
- 完成项目管理体验升级（第二轮）
  - 任务“查看”改为右侧抽屉，宽度 700px
  - 任务详情支持添加子任务
  - 评论 @提醒进入通知中心并可作为待办处理
  - 项目管理二级菜单补齐：
    - 项目清单
    - 任务清单
    - 关注项目
    - 关注任务
  - 项目/任务支持标签
  - 项目支持按关键词、状态、负责人、标签筛选
  - 任务支持按关键词、状态、负责人、标签筛选
  - 支持项目关注与任务关注
  - 全局布局升级为固定左侧+固定顶部，左侧支持收起/展开
  - UI 关键区域增加 emoji 装饰并收紧板块间距
- 新增数据库连通测试脚本（testDB）
  - PostgreSQL 连接检测脚本
  - MongoDB 连接检测脚本
  - MinIO 健康检查脚本
  - 一键汇总脚本
- 完成关系数据库主实现从 SQLite 切换到 PostgreSQL
  - 后端配置改为 `DATABASE_URL/DB_*` 驱动
  - SQLAlchemy 连接初始化支持 PostgreSQL 主实现
  - Docker Compose 增加 PostgreSQL 服务
  - 文档与环境变量模板同步更新
  - 测试环境保留独立 SQLite 以保证自动化测试可重复
- 初始化本地 git 仓库
- 首次提交并推送到远程仓库

### 当前仓库状态

- 已有需求文档
- 已有流程文档
- 已有页面原型草图
- 已开始前后端骨架开发
- 已进入项目管理模块第一版实现阶段
- 已进入项目管理模块第二轮体验完善阶段

### 当前阶段结论

当前阶段已从“项目骨架初始化阶段”进入“核心业务模块开发阶段”，登录、权限、用户管理、项目管理和通知待办已形成可持续迭代的基础。

### 下一步建议

1. 补充项目管理相关数据库设计文档
2. 补充项目管理 API 设计文档
3. 继续实现需求管理模块
4. 继续完善任务抽屉的编辑体验和字段校验
5. 完善 MinIO 对象存储接入（替换本地文件存储）
6. 开始 Docker Compose 联调验证
7. 继续扩展 RBAC 到更细粒度接口和数据范围

---

## 维护说明

后续每次有以下变化时，请同步补充本文件：

- 架构调整
- 新增模块设计
- 代码骨架初始化
- 接口设计完成
- 数据库结构确定
- Docker 部署方案调整
- 阶段开发完成
- 关键问题与解决方案

---

## 维护规则（新增，生效时间：2026-03-20 23:57:30 +08:00）

1. 开发记录只允许追加，不允许修改或删除历史记录。
2. 每次记录必须包含完整时间戳（格式：`YYYY-MM-DD HH:mm:ss +08:00`），不能只写日期。
3. 每次记录必须明确“本次大概修改了哪些内容”，至少包含：
   - 功能/模块范围
   - 关键文件或目录
   - 结果（完成/部分完成/待继续）

## 追加记录

### [2026-03-20 23:57:30 +08:00] 文档规则升级 + 性能与容错优化落档

- 修改范围：
  - 后端查询性能优化记录（项目/任务列表分页下推数据库、轻重查询拆分、索引补充）。
  - 前端容错优化记录（页面分区 loading、`Promise.allSettled`、局部失败不阻塞整页）。
  - 文档新增《性能优化与容错说明》并在文档总览中登记。
- 关键文件：
  - `backend/app/services/project_service.py`
  - `backend/app/api/v1/endpoints/projects.py`
  - `backend/app/db/init_db.py`
  - `frontend/src/views/projects/ProjectDetailView.vue`
  - `frontend/src/views/projects/ProjectListView.vue`
  - `frontend/src/views/projects/ProjectTaskListView.vue`
  - `docs/性能优化与容错说明.md`
  - `docs/README.md`
  - `docs/TODO.md`
- 状态：
  - 已完成并已提交 Git（commit：`0963206`）。

### [2026-03-21 00:32:46 +08:00] 项目归属与会议筛选能力增强
- 修改范围：
  - 放开项目创建权限：由“仅管理员创建”调整为“登录用户均可创建”。
  - 新增项目归属字段：`project_type`（`work`/`personal`），用于区分公司项目与个人项目。
  - 新增会议筛选能力：项目清单、任务清单均支持“排除个人项目（exclude_personal）”。
  - 补充“公司项目”入口与默认清单策略（我的清单默认进行中）。
- 关键文件：
  - `backend/app/models/project_management.py`
  - `backend/app/schemas/project_management.py`
  - `backend/app/api/v1/endpoints/projects.py`
  - `backend/app/services/project_service.py`
  - `backend/app/db/init_db.py`
  - `frontend/src/views/projects/ProjectListView.vue`
  - `frontend/src/views/projects/ProjectTaskListView.vue`
  - `frontend/src/router/index.js`
  - `backend/app/services/menu_service.py`
- 状态：
  - 已完成，待本次统一提交并推送远程。

### [2026-03-21 00:34:56 +08:00] 文档同步与交付整理
- 修改范围：
  - 同步更新开发文档，明确“个人项目 / 公司项目”并行管理方式。
  - 明确会议场景下“排除个人项目”筛选口径与使用方式。
  - 补充本次代码交付范围并准备推送。
- 关键文件：
  - `docs/开发记录.md`
  - `docs/开发计划.md`
  - `docs/详细权限说明文档.md`
- 状态：
  - 已完成，提交并推送中。
